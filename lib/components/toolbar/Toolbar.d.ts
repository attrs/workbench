import { ContainerOptions, Container } from '../../base';
export declare class ToolbarOptions extends ContainerOptions {
}
export declare class Toolbar extends Container {
    constructor(options: ToolbarOptions);
    create(options: ToolbarOptions): any;
}
